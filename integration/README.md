# Remove Dir
```
rm -rf scira-mcp-chat midday
```

# Create Dir
```
git clone https://github.com/react-pdf-viewer/react-pdf-viewer.git && cd react-pdf-viewer && rm -rf .git && cd ..
git clone https://github.com/zaidmukaddam/scira-mcp-chat.git && cd scira-mcp-chat && rm -rf .git && cd ..
git clone https://github.com/midday-ai/midday.git && cd midday && rm -rf .git && cd ..
```